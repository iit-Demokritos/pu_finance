# Scripts for testing/experiments etc.

To utilize the structure either **run from root**:

```cmd
python -m scripts.baseline_lr
```

Or manually set ```PYTHONPATH``` before running:
```cmd
export PYTHONPATH=$(pwd)
python scripts/baseline_lr.py
```

If you want to roll-out your own classifier check out the script in ```baseline_lr.py```.